class A inherits B {
  a : Int <- 10;
  f() : Int {{
    while a < 10 loop
      2 + 2 / 3 * 4 - 1
    pool;
    { a <- 3 * 5 + ~a / 2; ~a;};
  }};
};

class Main {
  a_obj: A <- new A;
  main() : Object {
    if (3 < 4) then
      a_obj.f()
    else
      a_obj.f() + 1
    fi
  };
};
